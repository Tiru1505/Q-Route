# ─── Core ──────────────────────────────────────────────────────
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
pydantic-settings>=2.6,<3
python-dotenv>=1.0,<2

# ─── Database ──────────────────────────────────────────────────
pymongo>=4.9,<5

# ─── Graph & computation ──────────────────────────────────────
networkx>=3.3,<4
numpy>=2.1,<3

# ─── Data processing (used by data loaders) ───────────────────
pandas>=2.2,<3
scipy>=1.13,<2

# ─── Testing ──────────────────────────────────────────────────
pytest>=8.3,<9
httpx>=0.27,<1
# Verifies Google sign-in tokens: signature, expiry, audience, issuer.
google-auth>=2.30,<3
requests>=2.31,<3

# ─── Optional: uncomment when team modules need them ──────────
# osmnx>=2.0,<3

# ─── Forecasting + vision ─────────────────────────────────────
# torch serves the trained LSTM (app/services/forecast_service.py) and backs
# Ultralytics. It was optional while nothing loaded a model; it no longer is.
torch>=2.3
# lap is required by ByteTrack, which is what turns per-frame detections into
# per-vehicle line crossings; without it Ultralytics downloads it on first use.
ultralytics>=8.4,<9
opencv-python>=4.10
lap>=0.5.12
python-multipart>=0.0.9
